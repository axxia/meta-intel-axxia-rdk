# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
	${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-axxia-snr-klm"
BBFILE_PATTERN_meta-axxia-snr-klm = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-axxia-snr-klm = "6"

# Location to populate with the RDK source code archive.
RDK_ARCHIVE_PATH := "${LAYERDIR}/downloads"
RDK_ARCHIVE := "rdk_klm_src.tar.xz"
